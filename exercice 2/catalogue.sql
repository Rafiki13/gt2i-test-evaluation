CREATE TABLE catalogue
(
    PRODUIT_POCLEUNIK  VARCHAR(50),
    PRODUIT_REF        VARCHAR(50),
    REFCIALE_ARCLEUNIK INT,
    REFCIALE_REFART    VARCHAR(50),
    REFCIALE_REFCAT    VARCHAR(50),
    POTRAD_DESI        TEXT,
    REFCIALE_CTVA      INT,
    FICTECH_MEMOCAT    TEXT,
    FICTECH_MEMONET    TEXT,
    PRODUIT_MARQUE     VARCHAR(2),
    PRODUIT_CLEP01     VARCHAR(2),
    PRODUIT_CLEP02     VARCHAR(3),
    PRODUIT_CLEP03     VARCHAR(3),
    PRODUIT_CLEP04     VARCHAR(3),
    PRODUIT_CLEP06     VARCHAR(1),
    PRODUIT_CLEP07     VARCHAR(1),
    PRODUIT_GCOLORIS   VARCHAR(50),
    PRODUIT_GTAILLE    FLOAT,
    PRODUIT_CLEP12     VARCHAR(1),
    REFCIALE_FICHEINA  INT,
    REFCIALE_MODTE     INT,
    PRODUIT_MODTE      INT,
    ARTICLE_POIDS      FLOAT,
    ARTICLE_HNORMEL    INT,
    ARTICLE_CATEG      INT,
    CONSTRAINT pk_catalogue PRIMARY KEY(PRODUIT_POCLEUNIK)
);